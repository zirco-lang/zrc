#include <stdio.zh>

fn main(argc: usize, argv: **u8) -> i32 {
    printf("Argument count: %d\n", argc);

    for (let i = 0usize; i < argc; i++) {
        printf("Argument %d: %s\n", i, argv[i]);
    }

    return 0;
}
