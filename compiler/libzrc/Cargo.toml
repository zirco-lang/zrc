[package]
name = "libzrc"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]
name = "zrc"
doc = false

[dependencies]
zrc = { path = "../zrc" }

[build-dependencies]
cbindgen = "0.29.2"
